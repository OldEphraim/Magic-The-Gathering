import { TextIconsPipe } from './text-icons.pipe';

describe('TextIconsPipe', () => {
  it('create an instance', () => {
    const pipe = new TextIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
